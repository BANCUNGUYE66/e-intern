specVersion: "v1beta"
serviceId: "e-intern"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "e-intern-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
